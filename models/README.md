# Models

Trained model files are not stored in the repository by default because they can be large.

Notebook C1 generates the main TinyPCGNet model file:

```text
tinypcgnet_v41_best.keras
```

Default output location in Google Drive:

```text
/content/drive/MyDrive/Msc_ML_project/results_v1_final
```
